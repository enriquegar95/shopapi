spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shopapi
    username: shopapi
    password: shopapi
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080